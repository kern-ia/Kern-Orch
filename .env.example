# Path to the external multi-provider LLM CLI invoked as a subprocess by agent nodes.
# When unset, agentrunner uses the deterministic stub (app runs without any LLM config).
KERN_AGENT_CLI=

# SQLite checkpoint database path.
KERN_CHECKPOINT_DB=./data/kern-orch.db
